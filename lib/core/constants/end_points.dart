

import 'base_urls.dart';

class EndPoints {
  static const String register = "${BaseUrls.baseUrl}/register";
  static const String login = "${BaseUrls.baseUrl}/login";
  static const String getUserInfo = "${BaseUrls.baseUrl}/user_info";
}
